import UserLocalStorageHelper from 'helpers/userLocalStorageHelper';

const userLocalStorageHelper = new UserLocalStorageHelper();
const userData = userLocalStorageHelper.getUserData();

export default userData;
